server { 
        listen 80 default_server;
        return 444;
}

server {
        server_name verahomeplex.duckdns.org;
        location / {
                proxy_pass http://localhost:${PLEX_PORT}/;      
        }
}